/*
    mongoose => a library that connects to MongoDB, and allows us to define schemas and models
    express.json() => a middleware that parses incoming requests with JSON payloads
    Note => a model that represents a collection in the database
    Note.find() => a method that returns all the documents in a collection
    model => a class that represents a collection in the database
    schema => a class that represents the structure of a document in a collection (used for validation)
    document => an instance of a model

    dotenv => a library that loads environment variables from a .env file into process.env

    cors => cross origin resource sharing
         => used to allow requests from other origins than the server's own origin
*/